```python
# 原来的代码
test = test.merge(target_mean, on=['id'], how='left')

.merge # pandas库中一个函数 合并DataFrame对象
on=['id'] # 指定id列为合并的依据列 merge函数根据id列匹配两个DataFrame中的行
how='left' # 使用左连接进行合并 保留test的所有行 右边target_mean没有匹配的行 就填充为NaN
```