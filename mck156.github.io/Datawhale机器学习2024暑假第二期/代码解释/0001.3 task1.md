```python
# 原来的代码
test[['id', 'dt', 'target']].to_csv('submit.csv', index = None)

[['id', 'dt', 'target']] # 选择id dt target三列 这是一个新的DataFrame对象
.to_csv # pandas库中函数 将DataFrame导出为CSV格式的文件
'submit.csv' # 导出的CSV文件名
index = None # 告诉to_csv函数导出时不包括
```