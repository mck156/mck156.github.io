# 缓存击穿
给某一个key设置了过期时间，当key过期的时候，恰好这时间点对这个key有大量的并发请求过来，这些并发的请求可能会瞬间把DB压垮
![[Pasted image 20240717113906.png]]
为什么会压垮数据库，不是已经同步了吗？
在缓存重建的过程中，比如消耗了50ms，大量数据过来数据库承受不住。
为什么50ms慢？
缓存的数据是多个表汇总的结果，分组统计更久，无论多久，大量数据过来还是承受不住。

# 解决方案
## 互斥锁


## 逻辑过期